[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  entrypoint = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "internal/db/sqlc"]
  delay = 1000

[log]
  time = false

[color]
  build = "yellow"
  runner = "green"
