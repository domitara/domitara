<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; Compose drives the actual UI colors via MaterialTheme. -->
    <style name="Theme.Domitara" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#2F95DC</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
